<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shoppy"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />
 <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.shoppy.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       <activity android:name="com.example.shoppy.Products"></activity>
       <activity android:name="com.example.shoppy.Productdetails"></activity>
        <activity android:name="com.example.shoppy.CartList"></activity>
         !-- Inbox Activity -->
        <activity android:name=".BuyDetails" />
 
        <!-- Outbox Activity -->
        <activity android:name=".TabActivity1" />
 
        <!-- Profile Activity -->
        <activity android:name=".TabActivity2" />
    </application>

</manifest>
